// Copy the initial recipe data from your MemStorage initialization
export const staticRecipes = [
    // Copy recipe data from server/storage.ts initializeSampleData() function
  ];
  
  export const staticCategories = [
    // Copy category data from server/storage.ts
  ];